version: '3.8'
services:
  privatespark:
    image: python:3.11-slim
    working_dir: /app
    volumes:
      - ./:/app
    command: bash -lc "pip install -r backend/requirements.txt && uvicorn privatespark.main:app --app-dir backend --host 0.0.0.0 --port 4173"
    ports:
      - "4173:4173"
